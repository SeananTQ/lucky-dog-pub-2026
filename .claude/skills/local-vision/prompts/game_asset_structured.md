分析这张游戏美术资源，严格按照以下 JSON 格式输出，不要输出任何其他内容：

```json
{
  "summary": "一句话概括这张图是什么",
  "size_px": "宽x高（从全画布尺寸判断）",
  "anchor": "推测此元素在引擎中应使用的锚点位置（center、top-left、bottom-left、top-right、bottom-right等）",
  "position","角色的鼻子在图片中的坐标"  
  "description": "详细描述：这是什么元素、在UI中什么用途、视觉特征（形状、主色、辅色、边框、圆角）、文字内容（如有）、在全画布中的位置"
}
```

注意：所有图片采用全画布导出，透明区域表示元素在画布中的实际位置，不要描述透明区域。
