---
name: luban-excel-guide
description: 当需要编辑Excel文件时，使用该技能。
---

## 概述

当接到编辑Excel的任务时，可以使用Excel MCP来编辑.xlsx文件，并遵守以下事项。


## 铁律
- 修改`xlsx`文件时，只允许修改文件名中包含`副本`、`copy`、的`xlsx`文件。

## 规则

- `luban-doc-main/`这个文件夹是官方文档，当你有不确定的东西时可以查看来确认细节
- 文档中会有一些图片链接，但有时你的模型可能不具有看图能力，此时你需要向用户请求协助
- 用户提供的xlsx或者csv中，除了表头之外，其他行的**第一列可能是注释**，你要留意这一点，避免后面看错列
- **Luban 4 列偏移**：数据行的第 A 列（注释列）应留空，ID 从第 B 列开始写入。填错会导致所有数据向左偏移一列
- **禁止对同一个 xlsx 文件并发写入**：excelize 后端不支持并行写操作，多个 `excel_write_to_sheet` 请求同时执行会互相干扰，导致已有数据被清空。必须串行写入，每次写完确认无误后再写下一段
- **用户交办新任务时先 re-read**：用户可能在上一个任务完成后手动编辑过 Excel（如插入/删除行）。每次用户提出新的填表需求时，应先用 `excel_describe_sheets` 或 `excel_read_sheet` 读取最新状态，再计算行号。同一轮连续写入过程中缓存是可靠的，无需反复读取



