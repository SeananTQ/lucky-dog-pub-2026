---
status: approved
last_editor: Codex
last_edit: 2026-08-26
---

# 多游戏模式系统说明

## 全局约定

### 术语

| 术语 | 说明 |
|------|------|
| **宿主窗口** | 唯一的 OS 级窗口，无边框、透明背景、置顶。切换模式时重设尺寸 |
| **游戏面板** | 扑克内容的渲染区，600×600 或 1200×1200 |
| **A 区** | 当前模式的主内容区域；桌宠模式为狗与 Counter，扑克模式为扑克游戏面板 |
| **胖窗口** | 宿主窗口内同时包含 A 区、面板缓冲和可选顶部附件的预留布局；面板开关不改变窗口尺寸 |

### 模式切换规则

- 三种模式共用一个宿主窗口，切换模式或确认其他 UI 倍率时重设窗口尺寸
- 同一模式内打开或关闭系统功能面板不会改变宿主窗口尺寸
- 按老板键（快捷键）时立即切到伪装模式，再按切回之前的模式
- 伪装模式下的模式切换按钮：伪装 → 游玩

## 桌宠伪装模式（老板来了）

又称 Boss Key 模式。切换到此模式后，宿主窗口按玩家确认的桌宠倍率重新计算，看起来是一个停留在桌面或任务栏上的桌宠。

### 视觉布局

```
┌────────────────────────────┐
│ 💬 盲盒提示或倒计时          │   ← 可选气泡
│             🐕            │   ← 小狗
│ 🔄  12:30 /  88.8K    ⚙   │   ← 矩形面板
└────────────────────────────┘
```

- 小狗（含穿戴装扮）趴在面板中间，纹理直接用现有素材缩放
- 面板左右两侧两个图标按钮：切换模式（🔄）、系统功能（⚙）
- Counter 可显示时钟、筹码或 Steam 昵称，也可隐藏
- 气泡显示盲盒倒计时、可开启状态、筹码不足和 Loading 等玩家可见状态
- 小狗、Counter、普通盲盒提示、桌宠开盒表演和启动 Rise 动画使用同一桌宠倍率
- 系统功能面板保持固定尺寸，不随桌宠倍率缩放

### 技术实现

- 小狗场景保留 0.25 基础缩放，设置页提供 `0.5x / 1x / 2x / 3x / 4x` 五档相对倍率
- 桌宠 A 区以 265×235 为基础设计尺寸，胖窗口按缩放后的 A 区、固定面板缓冲和盲盒表演顶部范围计算
- `ContentA`、桌宠 CanvasLayer、Bubble 层和桌宠盲盒表演共享设计坐标，不使用 SubViewport
- 玩家应用新倍率后需在十秒内确认保留，否则恢复应用前的倍率和窗口位置
- 详细规则见[桌宠倍率与窗口布局说明](./桌宠倍率与窗口布局说明.md)

### 文字显示模式

| 模式 | 内容 | 示例 |
|------|------|------|
| 时钟 | 当前系统时间 | `22:30` |
| 筹码 | 当前筹码数 | `41544` |
| Steam 昵称 | 当前 Steam 玩家昵称 | `Player` |
| 隐藏 | 不显示 Counter | - |

## 桌宠游玩模式

标准游玩模式。信息面板、扑克 A 区、系统功能面板缓冲和 Debug 构建中的可选 Mock 顶部附件都位于同一个宿主窗口内。

### 布局

```
┌────────────────────────────────────────────┐
│ ┌──────────────┐ ┌──────────────────────┐  │
│ │ 信息面板      │ │ 扑克游戏面板         │  │
│ │ 240×600      │ │ 600×600             │  │
│ │              │ │                     │  │
│ │ (筹码/牌型   │ │ (已实现 MVP 的      │  │
│ │  奖励/系统   │ │  扑克内容，50% 缩放) │  │
│ │  按钮)       │ │                     │  │
│ └──────────────┘ └──────────────────────┘  │
└────────────────────────────────────────────┘
     <- 840 ->
```

- 信息面板默认在 A 区左侧；宿主窗口左侧超出当前显示器工作区时，信息面板移动到 A 区右侧
- 扑克游戏面板 = 现有 MVP 内容通过 SubViewportContainer 渲染；设计尺寸为 600×600，并随其他 UI 倍率使用 `0.75x / 1x / 1.25x / 1.5x / 2x`
- 低倍率宿主窗口预留两行 A 区高度，高倍率只预留一行；两类布局的宽度均为“信息面板 + A 区 + 系统功能面板”
- 系统功能面板默认位于 A 区右侧；低倍率右侧不足时优先退到 A 区上方，高倍率或上方不足时覆盖 A 区
- 系统功能面板覆盖 A 区时显示 50% 黑色遮罩；点击遮罩关闭面板，遮罩在面板淡出结束后消失
- Debug 构建显示顶部 Steam Mock 面板时，宿主窗口计入其实际高度，且系统功能面板避开 Mock 面板
- 信息面板详细内容见[扑克游戏信息面板](./扑克游戏信息面板.md)
- 完整胖窗口与退避规则见[桌宠倍率与窗口布局说明](./桌宠倍率与窗口布局说明.md)

## 沉浸游玩模式

全屏窗口模式，采用完整的 1200×1200 美术素材。

- 扑克游戏面板：直接使用现有 MVP 的 1200×1200 布局，不缩放
- 牌桌和背景：九宫格横向拉伸以适配宽屏显示器（`TextureRect.StretchMode.Tile` / `Scale`）
- 系统功能面板：以**覆盖层（Overlay）**形式弹出，不跳转页面
  - 覆盖层半透明遮罩，系统面板浮在中央
  - 关闭覆盖层后无缝回到游戏画面
  - 复用现有 Overlay CanvasLayer 结构
- 播放完整 BGM + SFX
- 可通过老板键随时快速切换到伪装模式
- 窗口尺寸默认为显示器分辨率，可在设置中改为窗口模式

## 后续可扩展

- 伪装模式下播放等待动画（小狗打哈欠、睡觉等）
- 多显示器：自动选择光标所在屏幕打开沉浸模式
