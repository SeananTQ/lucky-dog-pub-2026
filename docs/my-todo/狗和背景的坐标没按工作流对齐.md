盘点完成。仍未按“场景参考物手调位置 + PSD 中心差值”实施的，主要有两类：

| 类型 | 结论 | 证据 / 建议 |
|---|---|---|
| Dog 小狗本体 | 未实施 | `Head / Eyes / Ears / Tongue` 都直接使用固定 PSD 基准坐标；移动 `DogArea` 里这些节点会被运行时覆盖。狗爪则只换贴图，不更新子 Sprite 的位置。应按各部件分别建立参考节点公式。 |
| Background 背景 | 未实施且有实际风险 | `GameManager` 直接把贴图赋给固定 1200×1200 的 `TextureRect`。现有 `HoneyHall`、`MidnightTheater`、`RedA` 的 PSD 裁切尺寸/坐标并不完全是 `(0,0,1200,1200)`，因此不满足工作流。应纳入参考/PSD 偏移，或明确要求这些背景统一导出为完整画布。 |
| CardBack / CardFace | 尚未接入装备显示 | 卡背仍固定为 `Basic.png`，卡面路径固定为 `v1/CardFace/Classic`；没有读取玩家已装备的 CardBack/CardFace。当前所有已有卡牌尺寸相同，因此不是“坐标公式缺失”造成的视觉问题，而是装备应用功能本身尚未完成。 |

已符合该工作流的部位：

- Headwear、Eyewear：刚完成迁移。
- Arm、Clothes、Accessory、BodyDecoration：`HandAreaController` 已使用参考节点 + PSD 中心差值。
- Refreshment：`ItemAreaController` 已以 Whisky 参考物计算 PSD 中心差值，并额外保持底边和气泡锚点。

不需要迁移的当前项：

- Table：现有实际配置的 11 张桌布在 PSD 中均为 `(0,677,1200,594)`，固定 `TextureRect` 落点可正确承载；若未来出现不同裁切尺寸/坐标的桌布，再迁移。
  
优先级建议：先迁移 **Dog 的头、眼、耳、舌头与两种爪子**；背景其次；CardBack/CardFace 则应作为独立的“卡牌换肤接入”任务处理。此次为静态代码和数据盘点，未做逐件运行时视觉验收。