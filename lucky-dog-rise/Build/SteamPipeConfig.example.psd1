@{
    # Lucky Dog Rise Playtest。脚本会拒绝其它 AppID。
    AppId = 4972240

    # 从 Steamworks 后台“SteamPipe > Depot”复制 Windows Depot ID。
    DepotId = 0

    # 只填写账号名。密码和 Steam Guard 验证码由 SteamCMD 交互输入，不写入文件。
    SteamAccount = 'your_steamworks_account'
}
